# Ignore everything by default
/*

# Un-ignore dist folder so it gets tracked
!/dist
!/dist/**

# Keep gitignore itself tracked
!.gitignore

# (old rules below are now redundant since /* already covers them,
# but harmless to keep for clarity if you ever flip this back)